// Authentication System Configuration
console.log('Loading auth configuration...');

export const AUTH_CONFIG = {
    // Set this to 'firebase' or 'sheets' to switch between authentication systems
    system: 'sheets'
};

console.log('Auth configuration loaded:', AUTH_CONFIG); 